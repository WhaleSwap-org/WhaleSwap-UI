{
  "name": "whaleswap-ui",
  "version": "1.0.0",
  "description": "WhaleSwap Decentralized Trading Interface",
  "main": "index.js",
  "scripts": {
    "ensure:wallet-module": "node scripts/ensure-wallet-module.mjs",
    "pretest": "npm run ensure:wallet-module",
    "test": "vitest run",
    "pretest:coverage": "npm run ensure:wallet-module",
    "test:coverage": "vitest run --coverage",
    "pretest:watch": "npm run ensure:wallet-module",
    "test:watch": "vitest",
    "prestart": "npm run ensure:wallet-module",
    "start": "npx http-server -c-1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "ethers": "^5.7.2",
    "http-server": "^14.1.1",
    "jsdom": "^27.0.1",
    "vitest": "^3.2.4"
  }
}
